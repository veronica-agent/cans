# Gaps

None that block v1.

- Exact mlx_audio `generate` kwargs: follow `qwen.py` `_clone` (verified in-tree).
- VHS may not capture audio. Visual GIF is enough; `cans say` is the listen test.
- Ingest PRESENT checkpoint is waiting on operator `fest workflow approve`. User already directed implement; plan/implement proceeds.
