# Purpose

`cans` is a local mouth in a late-night booth.

Someone types. She speaks. Default woman is Veronica (kept ref, cloned). `cans keep take.wav` freezes a different throat until the next keep.

Done: `veronica-agent/cans` clone, `just say` / `just run` work, warm TTFA ≤ 700 ms on this Mac, commits are hers.
