# Candidates

Score: **simple**, **visual**, **gap**, **on-brand**, **easier than the booth**, **does not leak the engine**.

Display name on the account stays **Obey Veronica**. Repos can still be named `cans` / `veronica` / whatever. She does not say Obey in the README quote.

## Ranked

### 1. `cans` — typed local mouth + booth TUI — **hero**

Put the cans on. Type. **Her** throat speaks (kept ref, cloned). Waveform. TTFA on screen. One `character.toml`. Charm TUI + VHS GIF (Fest already tapes CLIs this way).

- Gap: the mouth exists; the **room** does not.
- Easy: no mic, no barge-in, no Ollama, no sex UI. Clone is the one heavy piece.
- Brand: headphones, late night, her faces **and** her voice on first run.
- Any voice: `cans keep take.wav`, then that woman until you keep again. Not a 50-voice blender.
- Leak: ship the public ref wav the way we ship the face photos. Do not import `veronica-voice` or VoiceDesign.
- Fest: the festival that builds it is the ad.
- Kill: stock Kokoro “af_bella” with her picture. That is a different woman.

v2 parked inside the same repo later: `cans radio` (speak a festival step), or a one-file browser booth.

### 2. One-file browser booth (kokoro-js) — **parked as v2**

GitHub Pages, click, she talks, WASM stays in the browser. Stronger “I don't leave the machine” demo. Extra surface. Do not make v1 two UIs.

### 3. `fest-radio` — speak festival / agent events — **parked**

Stronger Fest ad, weaker Veronica. Niche (people who already run agents). Can be a `cans` subcommand later, not a second hero.

### 4. `same-face` — character-lock CLI — **parked**

Real pain. Backend is the problem (Imagine is closed; Flux/Comfy is a monster). After `cans`, maybe.

## Killed

| Idea | Why |
|------|-----|
| Public slice of `veronica-voice` | Private, 18+, Qwen, not ready. Fails “before the booth.” |
| Another coding agent | Saturated (~199k OpenCode). Wrong woman. |
| Uncensored local chatbot / SillyTavern lite | Crowded + NEVER (“local-first uncensored voice companion”). |
| Kokoro CLI with more flags | `kokoro-tts` already reads EPUBs. |
| Open WebUI skin | Not simple. Not her. |
| Stuffing code into `veronica-agent/veronica` | That repo is the **character** (all rights reserved). Keep it a face, not a toolchain. |

## Hard rules for any survivor

- No `projects/veronica-voice` import
- No VoiceDesign, no engine import. The public ref wav is the throat, like the photoreal is the face.
- No Obedience Corp / Lance / Samantha in her mouth
- No “star this” / “built with” in the pull-quote
- MIT (or similar) on the **tool**. Character repo stays all-rights.
